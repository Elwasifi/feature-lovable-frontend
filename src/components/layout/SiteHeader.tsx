import { MainNav } from "@/components/layout/MainNav";

/**
 * Unified site header for every interior page: the 8-item top navigation with
 * dropdown menus, search, language/currency switchers and account controls.
 */
export function SiteHeader() {
  return <MainNav />;
}
